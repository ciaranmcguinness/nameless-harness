[project]
name = "harness"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai-agents[any-llm]>=0.13.6",
    "prompt-toolkit>=3.0.52",
    "websocket-server>=0.6.4",
    "websockets>=16.0",
]
